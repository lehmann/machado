# Lightweight deps for CI. Enough to boot the FastAPI app and run the server
# with the deterministic fake-MT seam (MACHADO_FAKE_MT=1) — NO ctranslate2,
# spacy or transformers (heavy / GPU). CEFR falls back to the regex heuristic
# and segmentation to the regex splitter, which is exactly what the tests cover.
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27
pytest>=7
